import React from 'react';
import PostsContainer from './PostsContainer';

// New enhanced PostsPage component that uses the isolated posts module
const PostsPageNew = () => {
  return <PostsContainer />;
};

export default PostsPageNew;
